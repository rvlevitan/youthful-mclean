import React from 'react';

const SmallText = (props) => {
    const projectEntry = props.projectEntry;
    return (
            <section key={projectEntry.id} className="spacer">
            </section>
        );
};

export default SmallText;

